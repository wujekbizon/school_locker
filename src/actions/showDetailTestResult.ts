import { getQuestionById } from "@/server/queries";
import { FormattedAnswer } from "@/types/dbTypes";

// export async function (results:FormattedAnswer[]) {
//   const testQuestions  = results.map((result) => {

//     const question = await getQuestionById(result.questionId)

//     return question
//   })

//   return testQuestions
// }
